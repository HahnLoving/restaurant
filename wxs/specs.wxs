function getButtonStatus(selected, disabled) {
    if (disabled) {
        return 'disabled'
    } else if (selected){
        return 'active'
    }else {
        return 'selectable'
    }
}



module.exports = {
    getButtonStatus: getButtonStatus
}